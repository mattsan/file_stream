require 'spec_helper'

describe FileStream do
  it 'has a version number' do
    expect(FileStream::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
